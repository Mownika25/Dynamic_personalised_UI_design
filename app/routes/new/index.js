import { HtmlElement } from "cx/widgets";

import Controller from "./Controller";

export default (
	<cx>
		<div controller={Controller} style="padding: 20px">
			<p>Your shiny new board is being created...</p>
		</div>
	</cx>
);
